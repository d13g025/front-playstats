{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": "./",  // Garante que o TypeScript resolva corretamente o caminho base
    "paths": {
      "*": ["node_modules/*", "src/*"]  // Configura os caminhos para resolver arquivos dentro de "src"
    }
  }
}